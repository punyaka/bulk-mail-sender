# Project Decisions
> Auto-tracked by Sentinel


## Architecture
- [11:42:41 am] using an API key entered in the sidebar\n\"\"\"\n\nimport base64, pathlib, re","originalFile":"\"\"\"\nStreamlit Bulk-Mailer (Gmail API, no yagmail)\n────────────────────────────────────────────\n• Excel mail-merge — tags {Name}, {Salutation}, … in subject & body\n• Quill editors for header / body / footer\n• Optional header / footer images\n• Per-row PDF (URL or local path)\n• Auth once via Google OAuth; token stored at ~/.credentials/gmail_token.pickle\n\"\"\"\n\nimport base64, mimetypes, pathlib, re\nfrom io import BytesIO\n\nimport pandas as pd\nimport requests\nimport streamlit as st\nfrom streamlit_quill import st_quill\nfrom jinja2 import Environment, Undefined, select_autoescape\n\nimport time\nimport gdown\n\n\n# ─── Resend.com email helpers ──────────────────────────────────────────────────\nRESEND_API_URL = \"https://api.resend.com/emails\"\n\ndef send_via_resend(api_key, sender, to, subject, html_body, attachment=None):\n    \"\"\"Send one email via the Resend API. attachment = (filename, bytes) or None.\"\"\"\n    payload = {\"from\": sender, \"to\": [to], \"subject\": subject, \"html\": html_body}\n    if attachment:\n        fname, data = attachment\n        payload[\"attachments\"] = [{\n            \"filename\": fname,\n            \"content\": base64.b64encode(data).decode(),\n        }]\n    resp = requests.post(\n        RESEND_API_URL,\n        headers={\"Authorization\": f\"Bearer {api_key}\"},\n        json=payload,\n        timeout=30,\n    )\n    resp.raise_for_status()\n    return resp.json()\n\n\n# ─── Jinja env (single-brace tags) ─────────────────────────────────────────────\nclass SilentUndef(Undefined):\n    def _fail_with_undefined_error(self, *a, **kw):\n        return \"\"\njinja_env = Environment(\n    variable_start_string=\"{\",\n    variable_end_string=\"}\",\n    undefined=SilentUndef,\n    autoescape=select_autoescape(enabled_extensions=(\"html\",)),\n)\n\ndef clean_quill(html: str) -> str:\n    html = html.replace(\"&#123;\", \"{\").replace(\"&#125;\", \"}\")\n    html = re.sub(r\"<\\w+[^>]*>({\\w+})<\\/\\w+>\", r\"\\1\", html)\n    html = re.sub(r\"{ *([^} ]*?) *}\", r\"{\\1}\", html)\n    html = re.sub(r\"<p><br></p>\", \"\", html)\n    return html\n\ndef fix_inline_img_widths(html: str, width: int) -> str:\n    \"\"\"Force every <img ...> in Quill HTML to the chosen width.\"\"\"\n    return re.sub(\n        r'<img([^>]*?)>',\n        lambda m: (\n            f'<img{m.group(1)} style=\"width:{width}px;max-width:100%;'\n            'display:block;margin:0 auto;\" />'\n        ),\n        html,\n        flags=re.IGNORECASE,\n    )\n\ndef inline_p_spacing(html: str,\n                     margin: str = \"0 0 0 0\",\n                     lh: str = \"1.4\") -> str:\n    \"\"\"Add spacing *and justify alignment* to every <p …> tag.\"\"\"\n    pattern

## Architecture
- [11:47:01 am] using an API key entered in the sidebar\n9\t\"\"\"\n10\t\n11\timport base64, pathlib, re\n12\tfrom io import BytesIO\n13\t\n14\timport pandas as pd\n15\timport requests\n16\timport streamlit as st\n17\tfrom streamlit_quill import st_quill\n18\tfrom jinja2 import Environment, Undefined, select_autoescape\n19\t\n20\timport time\n21\timport gdown\n22\t\n23\t\n24\t# ─── Resend.com email helpers ──────────────────────────────────────────────────\n25\tRESEND_API_URL = \"https://api.resend.com/emails\"\n26\t\n27\tdef send_via_resend(api_key, sender, to, subject, html_body, attachment=None):\n28\t    \"\"\"Send one email via the Resend API. attachment = (filename, bytes) or None.\"\"\"\n29\t    payload = {\"from\": sender, \"to\": [to], \"subject\": subject, \"html\": html_body}\n30\t    if attachment:\n31\t        fname, data = attachment\n32\t        payload[\"attachments\"] = [{\n33\t            \"filename\": fname,\n34\t            \"content\": base64.b64encode(data).decode(),\n35\t        }]\n36\t    resp = requests.post(\n37\t        RESEND_API_URL,\n38\t        headers={\"Authorization\": f\"Bearer {api_key}\"},\n39\t        json=payload,\n40\t        timeout=30,\n41\t    )\n42\t    resp.raise_for_status()\n43\t    return resp.json()\n44\t\n45\t\n46\t# ─── Jinja env (single-brace tags) ─────────────────────────────────────────────\n47\tclass SilentUndef(Undefined):\n48\t    def _fail_with_undefined_error(self, *a, **kw):\n49\t        return \"\"\n50\tjinja_env = Environment(\n51\t    variable_start_string=\"{\",\n52\t    variable_end_string=\"}\",\n53\t    undefined=SilentUndef,\n54\t    autoescape=select_autoescape(enabled_extensions=(\"html\",)),\n55\t)\n56\t\n57\tdef clean_quill(html: str) -> str:\n58\t    html = html.replace(\"&#123;\", \"{\").replace(\"&#125;\", \"}\")\n59\t    html = re.sub(r\"<\\w+[^>]*>({\\w+})<\\/\\w+>\", r\"\\1\", html)\n60\t    html = re.sub(r\"{ *([^} ]*?) *}\", r\"{\\1}\", html)\n61\t    html = re.sub(r\"<p><br></p>\", \"\", html)\n62\t    return html\n63\t\n64\tdef fix_inline_img_widths(html: str, width: int) -> str:\n65\t    \"\"\"Force every <img ...> in Quill HTML to the chosen width.\"\"\"\n66\t    return re.sub(\n67\t        r'<img([^>]*?)>',\n68\t        lambda m: (\n69\t            f'<img{m.group(1)} style=\"width:{width}px;max-width:100%;'\n70\t            'display:block;margin:0 auto;\" />'\n71\t        ),\n72\t        html,\n73\t        flags=re.IGNORECASE,\n74\t    )\n75\t\n76\tdef inline_p_spacing(html: str,\n77\t                     margin: str = \"0 0 0 0\",\n78\t                     lh: str = \"1.4\") -> str:\n79\t    \"\"\"Add spacing *and justify alignment* to every <p …> tag.\"\"\"\n80\t    pattern
- [11:47:01 am] using an API key entered in the sidebar\n\"\"\"\n\nimport base64, pathlib, re\nfrom io import BytesIO\n\nimport pandas as pd\nimport requests\nimport streamlit as st\nfrom streamlit_quill import st_quill\nfrom jinja2 import Environment, Undefined, select_autoescape\n\nimport time\nimport gdown\n\n\n# ─── Resend.com email helpers ──────────────────────────────────────────────────\nRESEND_API_URL = \"https://api.resend.com/emails\"\n\ndef send_via_resend(api_key, sender, to, subject, html_body, attachment=None):\n    \"\"\"Send one email via the Resend API. attachment = (filename, bytes) or None.\"\"\"\n    payload = {\"from\": sender, \"to\": [to], \"subject\": subject, \"html\": html_body}\n    if attachment:\n        fname, data = attachment\n        payload[\"attachments\"] = [{\n            \"filename\": fname,\n            \"content\": base64.b64encode(data).decode(),\n        }]\n    resp = requests.post(\n        RESEND_API_URL,\n        headers={\"Authorization\": f\"Bearer {api_key}\"},\n        json=payload,\n        timeout=30,\n    )\n    resp.raise_for_status()\n    return resp.json()\n\n\n# ─── Jinja env (single-brace tags) ─────────────────────────────────────────────\nclass SilentUndef(Undefined):\n    def _fail_with_undefined_error(self, *a, **kw):\n        return \"\"\njinja_env = Environment(\n    variable_start_string=\"{\",\n    variable_end_string=\"}\",\n    undefined=SilentUndef,\n    autoescape=select_autoescape(enabled_extensions=(\"html\",)),\n)\n\ndef clean_quill(html: str) -> str:\n    html = html.replace(\"&#123;\", \"{\").replace(\"&#125;\", \"}\")\n    html = re.sub(r\"<\\w+[^>]*>({\\w+})<\\/\\w+>\", r\"\\1\", html)\n    html = re.sub(r\"{ *([^} ]*?) *}\", r\"{\\1}\", html)\n    html = re.sub(r\"<p><br></p>\", \"\", html)\n    return html\n\ndef fix_inline_img_widths(html: str, width: int) -> str:\n    \"\"\"Force every <img ...> in Quill HTML to the chosen width.\"\"\"\n    return re.sub(\n        r'<img([^>]*?)>',\n        lambda m: (\n            f'<img{m.group(1)} style=\"width:{width}px;max-width:100%;'\n            'display:block;margin:0 auto;\" />'\n        ),\n        html,\n        flags=re.IGNORECASE,\n    )\n\ndef inline_p_spacing(html: str,\n                     margin: str = \"0 0 0 0\",\n                     lh: str = \"1.4\") -> str:\n    \"\"\"Add spacing *and justify alignment* to every <p …> tag.\"\"\"\n    pattern
- [11:47:01 am] using an API key entered in the sidebar\n\"\"\"\n\nimport base64, pathlib, re\nfrom io import BytesIO\n\nimport pandas as pd\nimport requests\nimport streamlit as st\nfrom streamlit_quill import st_quill\nfrom jinja2 import Environment, Undefined, select_autoescape\n\nimport time\nimport gdown\n\n\n# ─── Resend.com email helpers ──────────────────────────────────────────────────\nRESEND_API_URL = \"https://api.resend.com/emails\"\n\ndef send_via_resend(api_key, sender, to, subject, html_body, attachment=None):\n    \"\"\"Send one email via the Resend API. attachment = (filename, bytes) or None.\"\"\"\n    payload = {\"from\": sender, \"to\": [to], \"subject\": subject, \"html\": html_body}\n    if attachment:\n        fname, data = attachment\n        payload[\"attachments\"] = [{\n            \"filename\": fname,\n            \"content\": base64.b64encode(data).decode(),\n        }]\n    resp = requests.post(\n        RESEND_API_URL,\n        headers={\"Authorization\": f\"Bearer {api_key}\"},\n        json=payload,\n        timeout=30,\n    )\n    resp.raise_for_status()\n    return resp.json()\n\n\n# ─── Jinja env (single-brace tags) ─────────────────────────────────────────────\nclass SilentUndef(Undefined):\n    def _fail_with_undefined_error(self, *a, **kw):\n        return \"\"\njinja_env = Environment(\n    variable_start_string=\"{\",\n    variable_end_string=\"}\",\n    undefined=SilentUndef,\n    autoescape=select_autoescape(enabled_extensions=(\"html\",)),\n)\n\ndef clean_quill(html: str) -> str:\n    html = html.replace(\"&#123;\", \"{\").replace(\"&#125;\", \"}\")\n    html = re.sub(r\"<\\w+[^>]*>({\\w+})<\\/\\w+>\", r\"\\1\", html)\n    html = re.sub(r\"{ *([^} ]*?) *}\", r\"{\\1}\", html)\n    html = re.sub(r\"<p><br></p>\", \"\", html)\n    return html\n\ndef fix_inline_img_widths(html: str, width: int) -> str:\n    \"\"\"Force every <img ...> in Quill HTML to the chosen width.\"\"\"\n    return re.sub(\n        r'<img([^>]*?)>',\n        lambda m: (\n            f'<img{m.group(1)} style=\"width:{width}px;max-width:100%;'\n            'display:block;margin:0 auto;\" />'\n        ),\n        html,\n        flags=re.IGNORECASE,\n    )\n\ndef inline_p_spacing(html: str,\n                     margin: str = \"0 0 0 0\",\n                     lh: str = \"1.4\") -> str:\n    \"\"\"Add spacing *and justify alignment* to every <p …> tag.\"\"\"\n    pattern
